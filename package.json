{
  "name": "puckdrop",
  "version": "1.0.0",
  "private": true,
  "description": "PuckDrop — NHL player explorer (school project). Static site + daily NHL data refresh.",
  "scripts": {
    "fetch": "node scripts/fetch-data.js",
    "fetch:resume": "node scripts/fetch-data.js --resume",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
